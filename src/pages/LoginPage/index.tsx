import * as React from 'react';
import { Text } from 'react-native';

export const LoginPage: React.FC = () => {
  return (
    <React.Fragment>
      <Text>LoginPage</Text>
    </React.Fragment>
  );
};
